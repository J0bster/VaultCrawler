export type ItemTemplate = {
    name: string;
    description: string;
    type: string;
    rarity: string;
}

export type IventoryItem = {
    name: string;
    description: string;
    type: string;
    rarity: string;
    quantity: number;
}